```mermaid
graph TD


    %% 3. 前端应用层
    subgraph FrontendLayer [前端应用层]
        Fetch[数据请求与解析]:::frontend
        Transform[Chart.js 数据格式转换]:::frontend
        
        subgraph Views [视图模块]
            View_Class[班级学情总览]:::frontend
            View_Ind[个人学情报告]:::frontend
            View_Risk[学业风险预警]:::frontend
            View_Pred[成绩预测展示]:::frontend
        end
    end

    %% 4. 可视化渲染层
    subgraph RenderLayer [Chart.js 渲染层]
        Chart_Bar[柱状图: 班级成绩分布]:::chartjs
        Chart_Radar[雷达图: 个人能力五维图]:::chartjs
        Chart_Line[折线图: 成绩趋势/预测]:::chartjs
        Chart_Doughnut[环形图: 风险等级占比]:::chartjs
    end

    %% 数据流向关系

    Fetch --> Transform
    
    Transform --> View_Class
    Transform --> View_Ind
    Transform --> View_Risk
    Transform --> View_Pred
    
    View_Class --> Chart_Bar
    View_Ind --> Chart_Radar
    View_Pred --> Chart_Line
    View_Risk --> Chart_Doughnut
    
```

教学管理模块交互图

```mermaid
sequenceDiagram
    participant T as 教师前端页面
    participant API as 后端API网关
    participant DB as 数据库/存储服务

    %% 场景1：课程管理
    Note over T, DB: 场景1：课程信息管理
    T->>API: GET /api/courses (获取课程列表)
    API->>DB: 查询课程表
    DB-->>API: 返回课程数据(JSON)
    API-->>T: 渲染课程卡片
    
    T->>API: POST /api/courses (新增/编辑课程信息)
    API->>DB: 更新课程记录
    DB-->>API: 返回操作结果
    API-->>T: 提示“保存成功”

    %% 场景2：作业全流程
    Note over T, DB: 场景2：作业发布与批改
    T->>API: POST /api/homework (发布作业)
    API->>DB: 存入作业表 & 推送通知
    DB-->>API: 发布成功
    
    rect rgb(232, 245, 233)
        Note right of T: 异步操作：批改作业
        T->>API: GET /api/homework/{id}/submissions (获取学生提交列表)
        API-->>T: 返回学生作业文件链接
        T->>API: POST /api/homework/{id}/grade (提交分数与评语)
        API->>DB: 更新成绩表
    end

    %% 场景3：考勤统计
    Note over T, DB: 场景3：考勤统计
    T->>API: POST /api/attendance/generate (生成考勤码)
    API->>DB: 创建考勤会话
    API-->>T: 返回二维码数据
    
    T->>API: GET /api/attendance/{session_id}/stats (轮询统计结果)
    API->>DB: 聚合签到数据
    DB-->>API: 返回已签/未签名单
    API-->>T: 渲染考勤报表(ECharts/Table)
```

课程信息管理流程

```mermaid
<!-- 横向流程图 -->
graph LR
    A[开始] --> B(进入课程管理页面)
    B --> C{前端请求课程列表}
    C -->D[后端返回课程数据]
    D --> E[展示课程列表]
    E --> F{点击具体课程}
    F --> G[获取课程详情]
    G --> H[编辑/更新信息]
    H --> I[后端保存并返回结果]
    I --> J[提示更新成功]
    J --> K[结束]
```

<br />

### **作业发布与批改流程**

```mermaid
<!-- 横向流程图 -->
graph LR
    Start((开始)) --> Step1[发布新作业]
    Step1 -->API1[后端创建作业]
    API1 --> Step2[查看学生提交列表]
    Step2 --> API2[后端返回提交记录]
    API2 --> Step3{选择批改}
    Step3 --> Step4[在线打分/写评语]
    Step4 --> API3[后端更新成绩]
    API3 --> End((结束))
```

### gRPC 机器学习微服务架构图（Mermaid）


```mermaid

graph LR
    %% 客户端
    Client[Next.js 后端客户端]
    
    %% 通信层
    GRPC[gRPC 通信<br/>Protocol Buffers]
    
    %% 服务端
    Server[Python gRPC 微服务]
    
    %% 模型资源
    Resource[模型资源<br/>模型/Scaler/特征/映射]

    %% 服务端核心子模块
    subgraph 服务端三大模块
        M1[模型加载模块]
        M2[接口实现模块]
        M3[服务配置模块]
    end

    %% 核心调用/依赖关系
    Client -->|gRPC请求| GRPC
    GRPC --> Server
    Resource --> M1
    M1 --> M2
    M2 --> M3
    M3 -->|绑定端口50051| GRPC

```